1) use underscore(done)
 user = new User(_.pick(req.body, ['name', 'email', 'password']));

2) use encrypt(done)
  const salt = await bcrypt.genSalt(10);
  user.password = await bcrypt.hash(user.password, salt);

3) login

4) use winston/ handle error

5) use .env for local and use secret key in user model generateAuthToken